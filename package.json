{
  "name": "menhera",
  "version": "0.6.7-rc4",
  "main": "dist",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bili --format umd --name index",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "bili": "^3.1.0",
    "menhera": "^0.6.4-rc2"
  },
  "dependencies": {
    "lodash.get": "^4.4.2",
    "lodash.has": "^4.5.2",
    "lodash.set": "^4.3.2"
  }
}
